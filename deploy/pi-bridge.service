[Unit]
Description=Pi Network Bridge Relay
After=network.target

[Service]
Type=simple
User=pi-bridge
WorkingDirectory=/opt/pi-network
EnvironmentFile=/opt/pi-network/.env
ExecStart=/usr/bin/node src/relay.js
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
